spring:
  datasource:
#    url: jdbc:h2:tcp://localhost/~/Developer/datasource/h2/test;MVCC=TRUE
    url: jdbc:h2:mem:test # 인메모리DB 사용
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true

logging: # logger로 출력
  level:
    org.hibernate.SQL: debug